@model IEnumerable<InterpolAlertApi.Dtos.TipoVittimaDto>

@{
    ViewData["Title"] = "Index";
}

<h2 class="text-success">@ViewBag.SuccessMessage</h2>
<div asp-validation-summary="ModelOnly" class="text-danger"></div>
<h1>Lista Tipo Vittime</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.TipoVittimaId)
                </th>*@
            <th>
                @Html.DisplayNameFor(model => model.NomeTipoVittima)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                @*<td>
                        @Html.DisplayFor(modelItem => item.TipoVittimaId)
                    </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.NomeTipoVittima)
                </td>
                <td>
                    <form asp-action="Delete" asp-route-tipoVittimaId="@item.TipoVittimaId" method="post">
                        <a class="btn btn-sm btn-primary" asp-controller="TipoVittima" asp-action="Edit" asp-route-tipoVittimaId="@item.TipoVittimaId">Edit</a>
                        <button class="btn btn-sm btn-danger" type="submit"
                                new { onclick="return confirm('Sei sicuro di voler eliminare @item.NomeTipoVittima?');">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
